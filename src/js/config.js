// Checkout configuration

define(function(require) {
  return {
    // For testing on sandbox
    apiHost: 'https://api-sandbox.trycelery.com',
    slug: '5432ce3a2aec950500d6239c',
    features: {
      taxes: true,
      coupons: false
    }
  };
});
